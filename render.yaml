services:
  - type: web
    name: krimass-relay
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node dist/relay-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        sync: false
