services:
  - type: web
    name: krimass-relay
    env: node
    buildCommand: npm install && npm run build
    startCommand: node dist/relay-server.js
    envVars:
      - key: PORT
        value: 3000
